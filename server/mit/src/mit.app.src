{application, mit, [
  {description, "mit"},
  {vsn, "8.0"},
  {modules, [mit,
            mit_dao,
			mit_sup,
            mit_event,
            mit_server,
			mit_app]},
  {registered, [mit]},
  {applications, [kernel, stdlib]},
  {env, []},
  {mod, {mit_app, []}}
]}.


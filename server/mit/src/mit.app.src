{application, mit, [
  {description, "mit"},
  {vsn, "8.1"},
  {modules, [mit,
            mit_app,
            mit_area,
            mit_board,
            mit_ctl,
            mit_event,
            mit_meta,
            mit_node,
            mit_port,
            mit_server,
			mit_sup]},
  {registered, [mit]},
  {applications, [kernel, stdlib]},
  {env, []},
  {mod, {mit_app, []}}
]}.


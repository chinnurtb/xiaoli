
$(function(){
  var sid = '#chart';
  var path = 'olt-0';
  
  // loadCircleTree(sid, path);
  // initZoomButtons(sid);
  // initTypeahead(sid);

  sid = '#tichart';
  loadInteractiveTree(sid, path);
});
